googleFolder.name=Google Cloud Storage
googleFolder.names=Google Cloud Storage

virtualFolder.names=Virtual Folders

name.name=Name
name.desc=The name to identify this mount when using this system.

mountPath.name=Mount Path
mountPath.desc=The path on which this folder is mounted within the virtual file system.

clientJson.name=Client JSON
clientJson.desc=The JSON content used as security credentials for this resource.

bucket.name=Bucket
bucket.desc=The bucket you want to access resources on.

filePath.name=Path
filePath.desc=The path within the bucket.